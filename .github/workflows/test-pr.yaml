name: Build and deploy PR application
on:
  pull_request:
    branches: [main]

jobs:
  cleanup:
    runs-on: ubuntu-latest
    if: ${{ github.actor != 'dependabot[bot]' }}
    steps:
        - name: Test PR run
          run: |
            echo "Kesa dia???"

      