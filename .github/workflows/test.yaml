name: My Workflow
on:
  workflow_dispatch:
jobs:
  my-job:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        namespace: [sbg, 360i]
    steps:
      - name: My step
        run: echo "My namespace is ${{ matrix.namespace }}"
